---
title: "survey_MASS_jellemzes"
output: html_document
---

```{r, echo = FALSE}
library(ggvis)
library(MASS)

data(survey)

plot <- survey %>%
  ggvis(~Height) %>%
  layer_histograms(width = input_slider(label = "Choose a binwidth:", min = 1, max = 20))

ggplotly(plot)
```

